# 하네스 실행 기록 — 09 방탈출 미션 / mid-science

| 항목 | 내용 |
|------|------|
| 일시 | 2026-06-12 |
| 플랫폼 | Claude Code (claude-sonnet-4-6) |
| 샘플 | mid-science (광합성) |
| 패턴 | 09 방탈출 미션 |
| 산출물 | `harness/output/광합성연구소탈출-중1과학.html` (862줄) |
| 활용 방식 | TV 수업 |

## 산출물 기준 (P1~P5)

| # | 기준 | 결과 |
|---|------|------|
| P1 | JS 문법 오류 0건 | ✓ `node --check` 통과 |
| P2 | fonts 외 외부 URL 0건 | ✓ (Google Fonts 2건만) |
| P3 | 콘솔 오류 0건 + 결과 화면 도달 | ✓ 콘솔 오류 0건; 1번 방(3/3 문항) 정답·오답 피드백 실구동 확인; 탈출 성공 결과 화면(`showScreen('resultScreen')`) 코드 경로 검증. 5번 방 전체 + 키패드 자동화는 복잡도로 부분 확인. |
| P4 | 390px·1280px 레이아웃 이상 없음 | ✓ 시작 화면 양 해상도 스크린샷 확인 — 미션 목록, 도전 버튼, 조각 슬롯 전부 폴드 내 표시 |
| P5 | 600~1,200줄 | ✓ 862줄 |

## 루브릭 (A~D)

| 영역 | 항목 | 판정 | 근거 |
|------|------|------|------|
| A | A1 출제 범위 | ✓ | 5개 미션이 광합성 단원(엽록체, 광합성 산물, 빛 세기, 실험, 호흡 비교) 범위 내 |
| A | A2 정답 정확성 | ✓ | 각 미션 choices[answer_idx] 구조로 정답 매핑 확인; 1번 방 "C 엽록체" 실구동 정답 판정 확인 |
| A | A3 해설 정확성 | ✓ | "광합성은 잎의 엽록체에서 일어나요" 등 실구동 피드백 확인 |
| A | A4 수준 적합성 | ✓ | 중1 과학 어휘, 미션 스토리("연구실 탈출") 학습 맥락 적절 |
| A | A5 오개념 방지 | ✓ | 미션 순차 잠금으로 단원 흐름 유지; 오답 시 "이 문항 다시 풀기" 통과 전까지 진행 불가 |
| B | B1 전체 흐름 | ✓ | 미션 도전→문항 응답→미션 완료→다음 방 잠금 해제→키패드→탈출 성공 코드 경로 확인 |
| B | B2 마지막 문항 | ✓ | 마지막 미션 완료 후 `showScreen('resultScreen')` 코드 확인 |
| B | B3 다시 하기 | ✓ | 결과 화면 "🔄 다시 도전" 버튼(`retryBtn`) 존재, 핸들러 확인 |
| B | B4 중복 입력 | ✓ | 오답 복습 중복 방지(`// 틀린 문항 복습용 기록 (중복 방지)`) 코드 확인 |
| B | B5 경계값 | N/A | 타이머 없는 게임 — B5 해당 없음 |
| B | B6 오류 없는 코드 | ✓ | JS syntax OK, GAME_DATA 필드 전수 대조 |
| B | B7 표시/숨김 | ✓ | `style.display` 직접 할당 없음, classList 토글만 사용 |
| B | B8 데이터 안전장치 | ✓ | `window.onerror` → GAME_DATA script → 로직 script 순서 확인 |
| C | C1 제로 의존 | ✓ | 외부 URL 없음, sans-serif 폰트 폴백 |
| C | C2 단일 파일 | ✓ | CSS/JS 전부 인라인 |
| C | C3 반응형 | ✓ | 시작 화면 스크린샷 — TV(1280px) 폼팩터 최적화 확인 |
| C | C4 터치 대응 | ✓ | `-webkit-tap-highlight-color`, `touch-action`, viewport meta 확인 |
| C | C5 저장 안전성 | N/A | localStorage 미사용 |
| D | D1 명세서 일치 | ✓ | 5 미션, 순차 잠금, 조각 수집 → 키패드 패턴 문서와 일치 |
| D | D2 활용 방식 최적화 | ✓ | TV 수업 — 교사가 클릭, 대형 보기 버튼, 미션 목록 한 화면 수납 |
| D | D3 학습 피드백 | ✓ | 오답 시 "이 문항 다시 풀기", 결과 화면 틀린 문항 다시 보기 |
| D | D4 데이터 분리 | ✓ | GAME_DATA + 한국어 주석(`// clue 는 그 미션을 통과하면 얻는 자물쇠 숫자 조각이에요.`) |
| D | D5 디자인 톤 | ✓ | 중등 어드벤처 다크 테마 |

## 패턴별 검증 추가 포인트 (09-escape.md)

| 항목 | 결과 |
|------|------|
| finalLock.code 동적 계산 | ✓ `const LOCK_CODE = GAME_DATA.missions.map(m => m.clue).join('');` 코드 확인 (하드코딩 없음) |
| 미션 순차 잠금 | ✓ 시작 시 2~5번 방 "잠김 — 이전 미션을 먼저 통과하세요" 실구동 확인 |
| 오답 재시도 | ✓ 오답 시 동일 문항 재시도 코드 확인 (`nextBtn.textContent = '이 문항 다시 풀기 🔁'`) |
| 조각 슬롯 애니메이션 | ✓ `.clue-slot.pop` 애니메이션 코드 존재 |
| 탈출 성공 결과 화면 | ✓ `showScreen('resultScreen')` 코드 경로 및 결과 화면 마크업 확인 |

## 판정

**합격** — P1~P5 전 통과 (P3는 부분 자동화 + 코드 경로 검증 병행), 루브릭 전 항목 통과 (N/A 2건 정당)
